//eslint-disable-next-line
import React from "react";

function ProgressBar() {
  return (
    <section className="fixed bottom-0 flex justify-between w-full text-center">
      <p>title</p>
      <div id="controls">
        {/* PREV btn w/ onClick
            play/pause btn w/ onClick
            NEXT btn w/ onClick*/}
      </div>
      <p>timestamp</p>
    </section>
  );
}
export default ProgressBar;
